{
  "name": "bauhaus-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "overrides": {
    "undici": ">=7.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260719.1",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.111.0"
  }
}
