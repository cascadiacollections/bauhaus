{
  "name": "bauhaus-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "overrides": {
    "undici": ">=7.24.0",
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260613.1",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.100.0"
  }
}
