{
  "name": "bauhaus-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "overrides": {
    "undici": ">=7.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.127.0"
  }
}
