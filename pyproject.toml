[project]
name = "bauhaus"
version = "0.1.0"
description = "CC0 daily stylized art service — AdaIN style transfer on public domain museum artwork"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0,<3.0",
    "torchvision>=0.15,<1.0",
    "pillow>=10.0,<12.0",
    "boto3>=1.28,<2.0",
    "requests>=2.31,<3.0",
]

[dependency-groups]
dev = []
